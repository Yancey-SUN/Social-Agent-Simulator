"use client";

import { useEffect, useMemo, useRef, useState } from "react";

type Intent = "explicit" | "implicit" | "unknown" | "negative";
type Persona = {
  id: number; name: string; age: number; city: string; job: string; archetype: string;
  topic: string; secondary: string; stage: string; preference: string; scenario: string;
  intent: Intent; readiness: number; energy: number; messages: number; episodes: number;
  discoverable: boolean; status: "chatting" | "ready" | "resting"; confidence: number;
  summary: string; memories: string[]; transcript: { from: "user" | "agent"; text: string }[];
};
type GroupMessage = { speaker: "user_a"|"user_b"|"agent_a"|"agent_b"; content: string };
type Match = { id: number; remoteId?: string; a: Persona; b: Persona; score: number; relation: string; resonance: string; value: string; status: "group" | "one-sided" | "research"; aYes: boolean; bYes: boolean; evidence: string[]; groupTranscript: GroupMessage[] };
type SimEvent = { id: number; time: string; type: string; user: string; message: string; tone: "blue" | "cream" | "green" | "gray" };
type Config = { population: number; rounds: number; groupRounds: number; autoGroup: boolean; messages: number; threshold: number; energyMax: number; surprise: number; seed: string };
type SharedRun = { id:string; createdAt:string|number; completedAt?:string|number|null; status:string; population:number; rounds:number; modelA:string; modelB:string; promptA:string; promptB:string; configJson:string; inputTokens:number; outputTokens:number; estimatedCostMicros:number };
type RunFailure = { id?:string; personaId:string; variant:string; stage:string; errorCode:string; message:string; createdAt?:string|number };
type ProviderName = "deepseek"|"openai"|"qwen"|"moonshot"|"siliconflow";

const FIRST = ["林漪","陈默","周野","沈知夏","许冬青","宋词","姜禾","陆遥","苏霁","唐棠","顾言","简宁","方澈","叶舟","何夕","温岚","谢一","孟川","余声","白榆","秦牧","夏屿","江弥","陶然","闻溪"];
const CITIES = ["上海","北京","杭州","深圳","成都","广州","南京","苏州","厦门","武汉"];
const JOBS = ["独立设计师","AI 产品经理","纪录片导演","品牌策略师","创业者","建筑师","研究员","心理咨询师","程序员","自由撰稿人","策展人","咖啡店主"];
const TOPICS = ["AI 与亲密关系","城市生活","独立创作","女性成长","户外与自然","创业低谷","长期主义","设计研究","心理健康","社区营造","音乐现场","个人表达"];
const STAGES = ["正在启动新项目","职业转型期","刚搬到新城市","寻找长期合作者","重新建立生活节奏","探索副业可能","作品发布前夕","想扩大认知边界"];
const ARCHETYPES = ["安静的建造者","好奇的连接者","敏锐的观察者","温柔的冒险家","务实的理想主义者","慢热的行动派"];
const SCENARIOS = ["深夜复盘","通勤闲聊","项目卡住","周末散步","搬家后的第一周","一次创作低潮","庆祝小小进展","对未来感到模糊"];
const PREFS = ["peer · 同行者","ahead · 过来人","contrast · 不同世界","complement · 互补伙伴"];
const BAZI_PRIORS = ["甲木：认准后持续向上，也不太愿意示弱","乙木：柔软但有韧性，擅长绕开阻力","丙火：热度藏不住，也容易消耗自己","丁火：细腻照顾近处的人，容易反复权衡","戊土：习惯扛事，慢热但关系稳定","己土：很会共情，常先接住别人的情绪","庚金：判断快，直接是保护色","辛金：对美与边界敏感，宁缺毋滥","壬水：思路快，不喜欢被框死","癸水：观察细，不一定立即说出口"];
const PROVIDER_OPTIONS:{id:ProviderName;label:string;model:string}[]=[{id:"deepseek",label:"DeepSeek",model:"deepseek-v4-flash"},{id:"openai",label:"OpenAI",model:"gpt-4.1-mini"},{id:"qwen",label:"通义千问",model:"qwen-plus"},{id:"moonshot",label:"Moonshot / Kimi",model:"moonshot-v1-8k"},{id:"siliconflow",label:"SiliconFlow",model:"Qwen/Qwen3-8B"}];

function hash(text: string) { let h = 2166136261; for (let i=0;i<text.length;i++) h = Math.imul(h ^ text.charCodeAt(i), 16777619); return h >>> 0; }
function rng(seed: string) { let s = hash(seed) || 1; return () => ((s = Math.imul(1664525, s) + 1013904223 >>> 0) / 4294967296); }
function clamp(n:number,min=0,max=1){return Math.min(max,Math.max(min,n))}
function pct(n:number){return `${Math.round(n*100)}%`}
function normalizeApiKey(value:string){return value.trim().replace(/^Bearer\s+/i,"").replace(/\s+/g,"")}
async function runPool<T>(items:T[],limit:number,worker:(item:T)=>Promise<void>,onError?:(item:T,error:unknown)=>void){let cursor=0;await Promise.all(Array.from({length:Math.min(limit,items.length)},async()=>{while(cursor<items.length){const item=items[cursor++];let lastError:unknown;for(let attempt=0;attempt<2;attempt++){try{await worker(item);lastError=undefined;break}catch(error){lastError=error;if(attempt===0)await new Promise(resolve=>setTimeout(resolve,900))}}if(lastError)onError?.(item,lastError)}}))}
function profileTerms(profile:any){const text=JSON.stringify(profile||{}).toLowerCase();const terms=new Set<string>(text.match(/[a-z0-9]{3,}|[\u4e00-\u9fff]{2}/g)||[]);for(const part of text.match(/[\u4e00-\u9fff]{3,}/g)||[])for(let i=0;i<part.length-1;i++)terms.add(part.slice(i,i+2));return terms}
function candidatePairs(pool:any[]){const candidates:any[]=[];for(let i=0;i<pool.length;i++)for(let j=i+1;j<pool.length;j++){const ta=profileTerms(pool[i].profile),tb=profileTerms(pool[j].profile);let overlap=0;for(const term of ta)if(tb.has(term))overlap++;const union=Math.max(1,new Set([...ta,...tb]).size);const sameCity=pool[i].persona.city===pool[j].persona.city?.08:0;const score=overlap/union+sameCity;candidates.push({a:pool[i],b:pool[j],retrievalScore:score})}candidates.sort((x,y)=>y.retrievalScore-x.retrievalScore||Number(x.a.persona.id)-Number(y.a.persona.id)||Number(x.b.persona.id)-Number(y.b.persona.id));const used=new Set<number|string>(),selected:any[]=[];for(const item of candidates){if(used.has(item.a.persona.id)||used.has(item.b.persona.id))continue;used.add(item.a.persona.id);used.add(item.b.persona.id);selected.push(item);if(selected.length>=Math.floor(pool.length/2))break}return selected}

function savedRunConfig(run:SharedRun){try{return JSON.parse(run.configJson||"{}")}catch{return {}}}
function savedRunStatus(run:SharedRun){if(run.status!=="running")return run.status;return Date.now()-new Date(run.createdAt).getTime()>30*60*1000?"failed":"running"}

function buildSimulation(config: Config, customNames: string[] = []) {
  const r = rng(config.seed);
  const users: Persona[] = Array.from({length: config.population},(_,i)=>{
    const name = customNames[i] || `${FIRST[i%FIRST.length]}${i>=FIRST.length?`·${Math.floor(i/FIRST.length)+1}`:""}`;
    const messages = Math.max(2, Math.round(config.messages*(.35+r()*1.25)));
    const intentRoll = r();
    const intent:Intent = intentRoll<.25?"explicit":intentRoll<.56?"implicit":intentRoll<.84?"unknown":"negative";
    const readiness = clamp(.18 + (1-Math.exp(-messages/11))*.68 + (r()-.5)*.18);
    const energy = Math.min(config.energyMax, (1-Math.exp(-messages/8))*config.energyMax + (r()<.18?1:0));
    const topic = TOPICS[Math.floor(r()*TOPICS.length)];
    const secondary = TOPICS[Math.floor(r()*TOPICS.length)];
    const city = CITIES[Math.floor(r()*CITIES.length)];
    const job = JOBS[Math.floor(r()*JOBS.length)];
    const stage = STAGES[Math.floor(r()*STAGES.length)];
    const scenario = SCENARIOS[Math.floor(r()*SCENARIOS.length)];
    const discoverable = r()>.18;
    const ready = readiness>=config.threshold && discoverable && (intent==="explicit"||intent==="implicit");
    return {id:i+1,name,age:22+Math.floor(r()*18),city,job,archetype:ARCHETYPES[Math.floor(r()*ARCHETYPES.length)],topic,secondary,stage,preference:PREFS[Math.floor(r()*PREFS.length)],scenario,intent,readiness,energy,messages,episodes:Math.max(1,Math.round(messages/6)),discoverable,status:ready?"ready":messages>config.messages*.48?"chatting":"resting",confidence:clamp(readiness-.08+r()*.15),summary:`${job}，目前${stage}。反复提到「${topic}」，在关系里更看重真实、节奏感与共同成长。`,memories:[`最近：${stage}`,`稳定兴趣：${topic}、${secondary}`,`社交偏好：${PREFS[Math.floor(r()*PREFS.length)]}`],transcript:[{from:"agent",text:`今晚想从哪里聊起？我在。`},{from:"user",text:`最近${stage}，我一直在想${topic}这件事。`},{from:"agent",text:`听起来它不只是一个话题，也和你现在所处的阶段有关。最卡住你的是什么？`},{from:"user",text:`可能是身边很少有人真的理解。我不急着要答案，更想遇见能聊深一点的人。`} ]};
  });
  const candidates = users.filter(u=>u.status==="ready" && u.energy>=2);
  const matches: Match[]=[];
  const used = new Set<number>();
  for(let i=0;i<candidates.length;i++){
    const a=candidates[i]; if(used.has(a.id)) continue;
    let best:Persona|undefined; let bestScore=0;
    for(let j=i+1;j<candidates.length;j++){
      const b=candidates[j]; if(used.has(b.id)||a.id===b.id) continue;
      const topical = a.topic===b.topic?.25:(a.secondary===b.topic||a.topic===b.secondary?.16:.06);
      const geo = a.city===b.city?.14:.04;
      const complement = a.preference.includes("contrast")||b.preference.includes("contrast")?.12:.08;
      const score=clamp(.34+topical+geo+complement+(r()-.5)*config.surprise);
      if(score>bestScore){bestScore=score;best=b}
    }
    if(best&&bestScore>.5){
      used.add(a.id);used.add(best.id);
      const aYes=r()<bestScore*.9,bYes=r()<bestScore*.88;
      matches.push({id:matches.length+1,a,b:best,score:bestScore,relation:a.preference.split(" · ")[0],resonance:`都在「${a.topic===best.topic?a.topic:`${a.topic} × ${best.topic}`}」附近寻找更真实的同行感`,value:`${a.name}带来一线经验，${best.name}提供不同视角；两人对关系节奏的期待接近。`,status:aYes&&bYes?"group":aYes||bYes?"one-sided":"research",aYes,bYes,evidence:[`Active State：${a.stage}`,`Stable：${best.topic}`,`${a.city===best.city?"同城可线下":"跨城市适合低压线上连接"}`],groupTranscript:[]});
    }
    if(matches.length>=Math.max(6,Math.round(config.population*.14)))break;
  }
  const events:SimEvent[]=[];let eid=1;
  users.slice(0,Math.min(users.length,26)).forEach((u,i)=>{
    const mm=String(8+Math.floor(i/3)).padStart(2,"0"),ss=String((i*17)%60).padStart(2,"0");
    events.push({id:eid++,time:`14:${mm}:${ss}`,type:i%4===0?"PROFILE_UPDATED":i%4===1?"SESSION_COMPACTED":i%4===2?"INTENT_JUDGED":"ENERGY_CHANGED",user:u.name,message:i%4===0?`Readiness ${pct(Math.max(0,u.readiness-.11))} → ${pct(u.readiness)}`:i%4===1?`${u.messages} 条对话压缩为 episode #${u.episodes}`:i%4===2?`Social Intent → ${u.intent}`:`Energy +${Math.min(1,u.energy).toFixed(1)} · 当前 ${u.energy.toFixed(1)}/${config.energyMax}`,tone:i%4===0?"blue":i%4===1?"cream":i%4===2?"green":"gray"});
  });
  matches.forEach((m,i)=>events.push({id:eid++,time:`15:${String(i+2).padStart(2,"0")}:12`,type:m.status==="group"?"GROUP_CREATED":"OPPORTUNITY_SCORED",user:`${m.a.name} ↔ ${m.b.name}`,message:m.status==="group"?`双方确认，四人群已建立 · score ${m.score.toFixed(2)}`:`关系机会完成双边研究 · score ${m.score.toFixed(2)}`,tone:m.status==="group"?"green":"blue"}));
  return {users,matches,events:events.reverse()};
}

const initialConfig:Config={population:20,rounds:4,groupRounds:4,autoGroup:true,messages:8,threshold:.62,energyMax:10,surprise:.24,seed:"VOUCH-REAL-020"};
const NAV=[['overview','◉','总览'],['run','✦','模拟运行'],['history','◷','历史实验'],['users','◎','用户宇宙'],['matches','⌁','匹配实验'],['logs','≡','事件日志']] as const;
const intentLabel:Record<Intent,string>={explicit:"Explicit",implicit:"Implicit",unknown:"Unknown",negative:"Negative"};

export default function Home(){
  const [tab,setTab]=useState<(typeof NAV)[number][0]>("overview");
  const [config,setConfig]=useState(initialConfig);
  const [universeSize,setUniverseSize]=useState(300);
  const [customNames,setCustomNames]=useState<string[]>([]);
  const [data,setData]=useState(()=>buildSimulation({...initialConfig,population:100}));
  const [running,setRunning]=useState(false); const [progress,setProgress]=useState(100);
  const [selectedUser,setSelectedUser]=useState(0); const [selectedMatch,setSelectedMatch]=useState(0);
  const [query,setQuery]=useState(""); const [logFilter,setLogFilter]=useState("ALL");
  const fileRef=useRef<HTMLInputElement>(null);
  const abortRef=useRef<AbortController|null>(null);
  const [runMode,setRunMode]=useState<"real"|"synthetic">("real");
  const [provider,setProvider]=useState<ProviderName>("deepseek");
  const [providerReady,setProviderReady]=useState<boolean|null>(null);
  const [serverProvider,setServerProvider]=useState<ProviderName>("deepseek");
  const [sessionKey,setSessionKey]=useState("");
  const [showKey,setShowKey]=useState(false);
  const [abEnabled,setAbEnabled]=useState(true);
  const [modelA,setModelA]=useState("deepseek-v4-flash"),[modelB,setModelB]=useState("deepseek-v4-flash");
  const [promptA,setPromptA]=useState("guardian-natural-v3"),[promptB,setPromptB]=useState("guardian-natural-v3");
  const [concurrency,setConcurrency]=useState(3);
  const [realEvents,setRealEvents]=useState<SimEvent[]>([]);
  const [realUsage,setRealUsage]=useState({input:0,output:0,micros:0,calls:0});
  const [runError,setRunError]=useState("");
  const [activeRunId,setActiveRunId]=useState("");
  const [experimentName,setExperimentName]=useState("关系实验 01");
  const [sharedRuns,setSharedRuns]=useState<SharedRun[]>([]);
  const [historyLoading,setHistoryLoading]=useState(false);
  const [sharedView,setSharedView]=useState(false);
  const [runFailures,setRunFailures]=useState<RunFailure[]>([]);
  const hasProvider=Boolean((providerReady&&provider===serverProvider)||normalizeApiKey(sessionKey).length>10);
  const ready=data.users.filter(u=>u.readiness>=config.threshold).length;
  const gated=data.users.filter(u=>u.readiness>=config.threshold&&(u.intent==="explicit"||u.intent==="implicit")&&u.discoverable).length;
  const groups=data.matches.filter(m=>m.status==="group").length;
  const active=data.users.filter(u=>u.status!=="resting").length;
  const mutual=data.matches.length?groups/data.matches.length:0;
  const phase=progress<24?"生成用户与场景":progress<49?"对话与 Memory compaction":progress<70?"Profile / Intent / Energy":progress<88?"Search Plan 与双边研究":"Sequential reveal 与建群";
  const dryRun=()=>{setRunning(true);setProgress(0);let p=0;const t=setInterval(()=>{p+=3+Math.random()*6;if(p>=100){clearInterval(t);setData(buildSimulation({...config,population:universeSize},customNames));setProgress(100);setRunning(false)}else setProgress(Math.round(p))},90)};
  const filteredUsers=useMemo(()=>data.users.filter(u=>`${u.name}${u.city}${u.job}${u.topic}`.toLowerCase().includes(query.toLowerCase())),[data,query]);
  const u=data.users[selectedUser]||data.users[0]; const match=data.matches[selectedMatch]||data.matches[0];
  const activeSharedRun=sharedRuns.find(x=>x.id===activeRunId);const activeRunMeta=activeSharedRun?savedRunConfig(activeSharedRun):{};
  const update=<K extends keyof Config>(k:K,v:Config[K])=>setConfig(c=>({...c,[k]:v}));
  const exportRun=()=>{const blob=new Blob([JSON.stringify({config,summary:{ready,gated,matches:data.matches.length,groups},...data},null,2)],{type:"application/json"});const a=document.createElement("a");a.href=URL.createObjectURL(blob);a.download=`vouch-sim-${config.seed}.json`;a.click();URL.revokeObjectURL(a.href)};
  const importPersonas=async(e:React.ChangeEvent<HTMLInputElement>)=>{const f=e.target.files?.[0];if(!f)return;const text=await f.text();let names:string[]=[];try{const x=JSON.parse(text);if(Array.isArray(x))names=x.map((v:any)=>typeof v==="string"?v:String(v.name||v.id||"")).filter(Boolean)}catch{names=text.split(/\r?\n/).slice(1).map(x=>x.split(",")[0].trim()).filter(Boolean)}if(names.length){const size=Math.min(300,Math.max(20,names.length));setCustomNames(names);setUniverseSize(size);setData(buildSimulation({...config,population:size},names));update("population",Math.min(config.population,size));}}
  const api=async(payload:Record<string,unknown>,signal?:AbortSignal)=>{const headers:Record<string,string>={"Content-Type":"application/json","X-LLM-Provider":provider};const cleanKey=normalizeApiKey(sessionKey);if(cleanKey)headers["X-LLM-API-Key"]=cleanKey;let res:Response;try{res=await fetch("/api/simulate",{method:"POST",headers,body:JSON.stringify({...payload,provider}),signal})}catch(error){if(error instanceof DOMException&&error.name==="AbortError")throw error;throw new Error("请求尚未到达实验服务器。请重新粘贴 API Key（系统会自动清除空格、换行和 Bearer 前缀），或刷新页面后重试")};let out:any;try{out=await res.json()}catch{throw new Error(`实验服务返回异常（HTTP ${res.status}）`)}if(!res.ok)throw new Error(out.error||`实验服务错误（HTTP ${res.status}）`);return out};
  async function loadSharedRun(runId:string,goToOverview=false){
    setHistoryLoading(true);setRunError("");
    try{
      const response=await fetch(`/api/simulate?run_id=${encodeURIComponent(runId)}`);const saved=await response.json();if(!response.ok||!saved.run)throw new Error(saved.error||"实验结果不存在");
      const run=saved.run as SharedRun;let stored:any={};try{stored=JSON.parse(run.configJson||"{}")}catch{}
      const nextConfig:Config={...initialConfig,...stored,population:run.population,rounds:run.rounds,seed:String(stored.seed||`SHARED-${run.id.slice(-6)}`)};
      const nextUniverse=Math.max(300,run.population,Math.min(300,Number(stored.universeSize||run.population)));const names=Array.isArray(stored.customNames)?stored.customNames:[];
      const base=buildSimulation({...nextConfig,population:nextUniverse},names);
      const profileById=new Map((saved.profiles||[]).filter((x:any)=>x.variant==="A").map((x:any)=>[String(x.personaId),x]));
      const messagesById=new Map<string,any[]>();for(const row of (saved.messages||[]).filter((x:any)=>x.variant==="A"&&!String(x.personaId).startsWith("group:")).sort((a:any,b:any)=>a.turnIndex-b.turnIndex)){const key=String(row.personaId);messagesById.set(key,[...(messagesById.get(key)||[]),row])}
      const groupMessagesByMatch=new Map<string,GroupMessage[]>();for(const row of (saved.messages||[]).filter((x:any)=>x.variant==="A"&&String(x.personaId).startsWith("group:")).sort((a:any,b:any)=>a.turnIndex-b.turnIndex)){const key=String(row.personaId).slice(6);groupMessagesByMatch.set(key,[...(groupMessagesByMatch.get(key)||[]),{speaker:String(row.speaker) as GroupMessage["speaker"],content:String(row.content)}])}
      base.users=base.users.slice(0,run.population).map(persona=>{const row:any=profileById.get(String(persona.id));const transcript=(messagesById.get(String(persona.id))||[]).map((m:any)=>({from:(m.speaker==="user"?"user":"agent") as "user"|"agent",text:String(m.content)}));if(!row)return {...persona,transcript,summary:transcript.length?"对话未完成，尚未生成画像。":"该用户未开始本次实验。",messages:transcript.length,memories:[],readiness:0,intent:"unknown" as Intent,status:transcript.length?"chatting":"resting"};let profile:any={};try{profile=JSON.parse(row.profileJson||"{}")}catch{}return {...persona,transcript,summary:String(profile.episode_summary||persona.summary),readiness:Number(profile.readiness||row.readiness||0),messages:transcript.length,memories:[...(profile.active_state||[]).slice(0,1).map((z:any)=>`最近：${z.claim}`),...(profile.stable_profile||[]).slice(0,1).map((z:any)=>`稳定倾向：${z.claim}`),`Social Intent：${profile.social_intent?.state||"unknown"}`],intent:(profile.social_intent?.state||"unknown") as Intent,status:Number(profile.readiness||row.readiness||0)>=nextConfig.threshold?"ready":"chatting"}});
      const byId=new Map(base.users.map(x=>[String(x.id),x]));
      const outcomeByMatch=new Map<string,any>();for(const out of (saved.outcomes||[]))outcomeByMatch.set(String(out.matchId),out);
      base.matches=(saved.matches||[]).map((row:any,i:number)=>{let research:any={};try{research=JSON.parse(row.researchJson||"{}")}catch{}const a=byId.get(String(row.personaAId))||base.users[0],b=byId.get(String(row.personaBId))||base.users[1]||base.users[0],out=outcomeByMatch.get(String(row.id));const groupTranscript=groupMessagesByMatch.get(String(row.id))||[];const aYes=Boolean(out?.acceptedA)||row.status==="group",bYes=Boolean(out?.acceptedB)||row.status==="group";return {id:i+1,remoteId:String(row.id),a,b,score:Number(row.score||0),relation:String(row.relationType||"unexpected"),resonance:String(research.resonance||""),value:String(research.mutual_value||""),status:(groupTranscript.length||row.status==="group")?"group":aYes||bYes?"one-sided":"research",aYes,bYes,evidence:Array.isArray(research.evidence)?research.evidence:[],groupTranscript}});
      base.events=[{id:1,time:new Date(run.completedAt||run.createdAt).toLocaleTimeString("zh-CN",{hour12:false}),type:"RUN_REPLAYED",user:"SHARED DATABASE",message:`已加载 ${run.population} 人实验 · ${base.matches.length} 个关系机会`,tone:"green"}];
      setConfig(nextConfig);setUniverseSize(nextUniverse);setCustomNames(names);setData(base);setActiveRunId(run.id);setExperimentName(String(stored.experimentName||`实验 ${run.id.slice(-6)}`));setProvider((stored.provider||"deepseek") as ProviderName);setModelA(run.modelA);setModelB(run.modelB);setPromptA(run.promptA);setPromptB(run.promptB);setAbEnabled(stored.abEnabled!==false);setRealUsage({input:run.inputTokens||0,output:run.outputTokens||0,micros:run.estimatedCostMicros||0,calls:(saved.messages||[]).length+(saved.profiles||[]).length+(saved.matches||[]).length});setRunFailures(saved.failures||[]);setRealEvents(base.events);setSelectedUser(0);setSelectedMatch(0);setProgress(100);setSharedView(true);if(goToOverview)setTab("overview");
    }catch(e){setRunError(e instanceof Error?e.message:"共享实验加载失败")}finally{setHistoryLoading(false)}
  }
  async function refreshSharedRuns(autoLoad=false){try{const response=await fetch("/api/simulate");const saved=await response.json();setProviderReady(Boolean(saved.configured));setServerProvider((saved.defaultProvider||"deepseek") as ProviderName);const runs=(saved.runs||[]) as SharedRun[];setSharedRuns(runs);const latest=runs.find(x=>["completed","partial"].includes(x.status));if(autoLoad&&latest)await loadSharedRun(latest.id,true)}catch{setProviderReady(false)}}
  useEffect(()=>{void refreshSharedRuns(true)},[]);
  const resizeUniverse=(size:number)=>{setUniverseSize(size);setData(buildSimulation({...config,population:size},customNames));if(config.population>size)update("population",size)};
  const pushRealEvent=(type:string,user:string,message:string,tone:SimEvent["tone"]="blue")=>setRealEvents(prev=>[{id:Date.now()+Math.random(),time:new Date().toLocaleTimeString("zh-CN",{hour12:false}),type,user,message,tone},...prev].slice(0,80));
  const addUsage=(x:any)=>x?.usage&&setRealUsage(v=>({input:v.input+(x.usage.input||0),output:v.output+(x.usage.output||0),micros:v.micros+(x.usage.micros||0),calls:v.calls+1}));
  const saveFailure=async(runId:string,failure:RunFailure)=>{setRunFailures(prev=>[...prev,failure]);try{await api({action:"failure",experimentId:runId,...failure})}catch{return}};
  const realRun=async()=>{
    if(!hasProvider){setRunError("请配置站点 Secret，或输入当前会话临时 DeepSeek Key");return}
    setRunning(true);setProgress(0);setRunError("");setRealEvents([]);setRunFailures([]);setRealUsage({input:0,output:0,micros:0,calls:0});
    const controller=new AbortController();abortRef.current=controller;let runIdForStatus="";let failedJobs=0;const failureWrites:Promise<void>[]=[];
    try{
      const created=await api({action:"create_run",population:config.population,rounds:config.rounds,concurrency,modelA,modelB,promptA,promptB,config:{...config,provider,experimentName:experimentName.trim()||"未命名实验",universeSize,customNames,abEnabled}},controller.signal);runIdForStatus=created.runId;setActiveRunId(created.runId);setSharedView(false);
      const seeds=buildSimulation({...config,population:universeSize},customNames).users.slice(0,config.population).map((p,i)=>({...p,baziPrior:BAZI_PRIORS[i%BAZI_PRIORS.length]}));
      const jobs=seeds.flatMap(persona=>(abEnabled?["A","B"]:["A"]).map(variant=>({persona,variant,stage:"conversation_start"})));
      const completed:any[]=[];let done=0,conversationsDone=0;
      await runPool(jobs,concurrency,async(job)=>{
        const {persona,variant}=job;
        const model=variant==="A"?modelA:modelB,promptVersion=variant==="A"?promptA:promptB;const history:any[]=[];
        pushRealEvent("AGENT_STARTED",persona.name,`Variant ${variant} · ${model}`,"gray");
        let conversationError:unknown=null;
        for(let turn=0;turn<config.rounds;turn++){
          try{
            job.stage=`user_turn_${turn+1}`;
            const userOut=await api({action:"turn",role:"user",experimentId:created.runId,persona,history,variant,model,promptVersion:"user-natural-v2",turnIndex:turn*2},controller.signal);addUsage(userOut);for(const text of (userOut.texts||[userOut.text])){history.push({speaker:"user",content:text});pushRealEvent("USER_MESSAGE",persona.name,text,"cream")}
            job.stage=`agent_turn_${turn+1}`;
            const agentOut=await api({action:"turn",role:"agent",experimentId:created.runId,persona,history,variant,model,promptVersion,turnIndex:turn*2+1},controller.signal);addUsage(agentOut);for(const text of (agentOut.texts||[agentOut.text])){history.push({speaker:"agent",content:text});pushRealEvent("AGENT_MESSAGE",persona.name,text,"blue")}
          }catch(error){conversationError=error;break}
        }
        if(history.length)conversationsDone++;
        if(conversationError){failedJobs++;const message=conversationError instanceof Error?conversationError.message:"对话中断";failureWrites.push(saveFailure(created.runId,{personaId:String(persona.id),variant,stage:job.stage,errorCode:"CONVERSATION_INTERRUPTED_PROFILE_CONTINUED",message:`对话在 ${job.stage} 中断；已使用现有 ${history.length} 条消息继续生成阶段性画像。${message}`}));pushRealEvent("CONVERSATION_INTERRUPTED",persona.name,`${history.length} 条消息后中断，继续生成画像`,"cream")}
        if(!history.length)throw conversationError||new Error("会话未产生任何消息");
        job.stage="profile";
        const profileOut=await api({action:"profile",experimentId:created.runId,persona,history,variant,model,partialSession:Boolean(conversationError)},controller.signal);addUsage(profileOut);completed.push({persona,variant,history,profile:{...profileOut.profile,partial_session:Boolean(conversationError)}});pushRealEvent("PROFILE_UPDATED",persona.name,`Readiness ${pct(Number(profileOut.profile.readiness||0))}${conversationError?" · 阶段性画像":""}`,"green");done++;setProgress(Math.round(done/jobs.length*68));
      },({persona,variant,stage},error)=>{failedJobs++;done++;const message=error instanceof Error?error.message:"未知错误";failureWrites.push(saveFailure(created.runId,{personaId:String(persona.id),variant,stage,errorCode:"AGENT_JOB_FAILED",message}));pushRealEvent("AGENT_FAILED",persona.name,`Variant ${variant} · ${stage} · ${message}`,"gray");setProgress(Math.round(done/jobs.length*78))});
      if(!completed.length)throw new Error(conversationsDone?`${conversationsDone} 个对话已经完整写入消息库，但 Profile 画像均生成失败；请查看各用户的 profile 失败原因`:"所有对话生成任务均失败；请查看每个用户的具体失败轮次与 Provider 错误");
      const realMatches:Match[]=[];const variants=abEnabled?["A","B"]:["A"];
      const matchJobs=variants.flatMap(variant=>candidatePairs(completed.filter(x=>x.variant===variant&&Number(x.profile.readiness||0)>=config.threshold)).map(pair=>({...pair,variant})));
      if(!matchJobs.length)pushRealEvent("MATCH_POOL_EMPTY","SYSTEM",`没有形成候选对：至少需要同一 Variant 下 2 位 Readiness ≥ ${pct(config.threshold)} 的用户`,"cream");
      let matchDone=0;await runPool(matchJobs,Math.min(3,concurrency),async({a,b,variant})=>{const model=variant==="A"?modelA:modelB;const out=await api({action:"match",experimentId:created.runId,variant,model,personaAId:a.persona.id,personaBId:b.persona.id,profileA:a.profile,profileB:b.profile},controller.signal);addUsage(out);const rr=out.research,score=Number(rr.score||0),qualifies=rr.recommend===true||score>=.55;const item:Match={id:realMatches.length+1,remoteId:out.matchId,a:a.persona,b:b.persona,score,relation:String(rr.relationship_type||"unexpected"),resonance:String(rr.resonance||""),value:String(rr.mutual_value||""),status:"research",aYes:false,bYes:false,evidence:Array.isArray(rr.evidence)?rr.evidence:[],groupTranscript:[]};pushRealEvent("OPPORTUNITY_SCORED",`${a.persona.name} × ${b.persona.name}`,`Variant ${variant} · score ${score.toFixed(2)}${qualifies?" · Agent 建议认识":" · 暂不推荐"}`,qualifies?"green":"gray");
        if(config.autoGroup&&qualifies){try{const opened=await api({action:"create_group",experimentId:created.runId,matchId:out.matchId,variant,personaA:a.persona,personaB:b.persona,research:rr},controller.signal);item.groupTranscript=(opened.messages||[]) as GroupMessage[];item.status="group";item.aYes=true;item.bYes=true;pushRealEvent("GROUP_CREATED",`${a.persona.name} × ${b.persona.name}`,`双方确认，四人群已建立 · 继续 ${config.groupRounds} 轮群聊`,"green");for(let round=0;round<config.groupRounds;round++){const baseIndex=2+round*4;const historyForApi=()=>item.groupTranscript.map(message=>({speaker:message.speaker==="user_a"?a.persona.name:message.speaker==="user_b"?b.persona.name:message.speaker==="agent_a"?`${a.persona.name}的守护者`:`${b.persona.name}的守护者`,content:message.content}));const turnA=await api({action:"group_turn",experimentId:created.runId,matchId:out.matchId,variant,model,persona:a.persona,peer:b.persona,speaker:"user_a",turnIndex:baseIndex,history:historyForApi()},controller.signal);addUsage(turnA);item.groupTranscript.push({speaker:"user_a",content:turnA.text});if(/@(?:守护者|小精灵|agent|Agent|苔苔)/.test(turnA.text)){const agentA=await api({action:"group_agent",experimentId:created.runId,matchId:out.matchId,variant,model,speaker:"agent_a",turnIndex:baseIndex+1,history:historyForApi()},controller.signal);if(!agentA.skipped){addUsage(agentA);item.groupTranscript.push({speaker:"agent_a",content:agentA.text})}}const turnB=await api({action:"group_turn",experimentId:created.runId,matchId:out.matchId,variant,model,persona:b.persona,peer:a.persona,speaker:"user_b",turnIndex:baseIndex+2,history:historyForApi()},controller.signal);addUsage(turnB);item.groupTranscript.push({speaker:"user_b",content:turnB.text});if(/@(?:守护者|小精灵|agent|Agent|苔苔)/.test(turnB.text)){const agentB=await api({action:"group_agent",experimentId:created.runId,matchId:out.matchId,variant,model,speaker:"agent_b",turnIndex:baseIndex+3,history:historyForApi()},controller.signal);if(!agentB.skipped){addUsage(agentB);item.groupTranscript.push({speaker:"agent_b",content:agentB.text})}}}pushRealEvent("GROUP_CHAT_COMPLETED",`${a.persona.name} × ${b.persona.name}`,`${item.groupTranscript.length} 条群消息已保存`,"green")}catch(error){failedJobs++;const message=error instanceof Error?error.message:"群聊模拟失败";failureWrites.push(saveFailure(created.runId,{personaId:`${a.persona.id},${b.persona.id}`,variant,stage:"group_chat",errorCode:"GROUP_CHAT_PARTIAL",message}));pushRealEvent("GROUP_CHAT_PARTIAL",`${a.persona.name} × ${b.persona.name}`,message,"cream")}}
        realMatches.push(item);matchDone++;setProgress(68+Math.round(matchDone/Math.max(1,matchJobs.length)*28))},({a,b,variant},error)=>{failedJobs++;matchDone++;const message=error instanceof Error?error.message:"匹配失败";failureWrites.push(saveFailure(created.runId,{personaId:`${a.persona.id},${b.persona.id}`,variant,stage:"matching",errorCode:"MATCH_FAILED",message}));setProgress(68+Math.round(matchDone/Math.max(1,matchJobs.length)*28));pushRealEvent("MATCH_FAILED",`${a.persona.name} × ${b.persona.name}`,message,"gray")});
      const primary=new Map(completed.filter(x=>x.variant==="A").map(x=>[x.persona.id,x]));const base=buildSimulation({...config,population:config.population},customNames);base.users=base.users.map(p=>{const x=primary.get(p.id);if(!x)return {...p,transcript:[],summary:"该会话未完成。",messages:0,memories:[],readiness:0,intent:"unknown" as Intent,status:"resting" as const};const pr=x.profile;return {...p,transcript:x.history.map((m:any)=>({from:m.speaker as "user"|"agent",text:m.content})),summary:String(pr.episode_summary||p.summary),readiness:Number(pr.readiness||0),memories:[...(pr.active_state||[]).slice(0,1).map((z:any)=>`最近：${z.claim}`),...(pr.stable_profile||[]).slice(0,1).map((z:any)=>`稳定倾向：${z.claim}`),`Social Intent：${pr.social_intent?.state||"unknown"}`],intent:(pr.social_intent?.state||"unknown") as Intent,status:Number(pr.readiness||0)>=config.threshold?"ready":"chatting"}});base.matches=realMatches;base.events=[];setData(base);
      await Promise.all(failureWrites);const finalStatus=failedJobs?"partial":"completed";await api({action:"complete",experimentId:created.runId,status:finalStatus});setProgress(100);pushRealEvent("RUN_COMPLETED","SYSTEM",`${completed.length}/${jobs.length} 个 Agent 会话完成 · ${realMatches.length} 个机会`,failedJobs?"cream":"green");await refreshSharedRuns(false);
    }catch(e){const message=e instanceof Error?e.message:"运行失败";const aborted=e instanceof DOMException&&e.name==="AbortError";if(runIdForStatus){try{if(!aborted)await saveFailure(runIdForStatus,{personaId:"SYSTEM",variant:"A",stage:"experiment",errorCode:"RUN_FAILED",message});await api({action:"complete",experimentId:runIdForStatus,status:aborted?"aborted":"failed"})}catch{}}if(!aborted)setRunError(message);await refreshSharedRuns(false)}finally{setRunning(false);abortRef.current=null}
  };
  const recordOutcome=async(checkpoint:string,alive:boolean)=>{if(!match?.remoteId||!activeRunId)return;try{await api({action:"outcome",experimentId:activeRunId,matchId:match.remoteId,checkpoint,acceptedA:true,acceptedB:true,messagesExchanged:alive?12:0,relationshipAlive:alive,note:"manual eval"});pushRealEvent("OUTCOME_SAVED",`${match.a.name} × ${match.b.name}`,`${checkpoint} · ${alive?"关系持续":"未持续"}`,alive?"green":"gray")}catch(e){setRunError(e instanceof Error?e.message:"Outcome 保存失败")}};
  return <main>
    <aside className="rail"><div className="brand"><span>V</span><em>Vouch Lab</em><b>β</b></div><nav aria-label="主导航">{NAV.map(([id,icon,label])=><button key={id} className={`nav ${tab===id?'active':''}`} onClick={()=>setTab(id)}><i>{icon}</i><span>{label}</span>{id==="logs"&&<small>{(realEvents.length||data.events.length)}</small>}</button>)}</nav><div className="rail-sep"/><button className="nav" onClick={exportRun}><i>⇩</i><span>导出本轮</span></button><div className="rail-foot"><i className={hasProvider?"":"offline"}/> {hasProvider?`${PROVIDER_OPTIONS.find(x=>x.id===provider)?.label||provider} 已连接${sessionKey?" · 临时 Key":""}`:"等待 LLM API Key"}<br/><small>{universeSize} 人宇宙 · 本轮 {config.population} 人</small></div></aside>
    <section className="canvas">
      <header><div><p className="eyebrow">SOCIAL AGENT SIMULATOR · {config.seed}</p><h1>{tab==="overview"?`${data.users.length} 个小精灵，正在真正对话。`:tab==="run"?"让一次关系实验可复现。":tab==="history"?"一次运行，所有人共享结果。":tab==="users"?"每个人，都在被慢慢理解。":tab==="matches"?"不是相似的人，而是值得认识的人。":"看见系统做出的每一个判断。"}</h1><p className="sub">{tab==="overview"?"DeepSeek Conversation → Memory → Profile → Matching → Outcome。":tab==="run"?"调整模型与 Prompt，重放真实对话和匹配链路。":tab==="history"?"共享数据库保存完整实验；查看回放不会调用模型。":tab==="users"?"检查真实对话、Memory、Intent 与画像证据。":tab==="matches"?"研究关系假设、双边价值和长期结果。":"全链路 trace：可筛选、可解释、可导出。"}</p></div><div className="header-actions"><button className="ghost" onClick={()=>fileRef.current?.click()}>↑ 导入人设</button><input ref={fileRef} hidden type="file" accept=".json,.csv" onChange={importPersonas}/><button className="run" onClick={()=>setTab("run")} disabled={running}>{running?`运行中 ${progress}%`:"▶ 新建实验"}</button></div></header>
      {tab==="run"&&<ProviderPicker provider={provider} onChange={next=>{setProvider(next);setSessionKey("");setShowKey(true);const defaultModel=PROVIDER_OPTIONS.find(x=>x.id===next)?.model||"deepseek-v4-flash";setModelA(defaultModel);setModelB(defaultModel)}}/>}
      {tab==="overview"&&<>
        {sharedView&&<div className="shared-banner"><span><b>共享实验回放</b><small>其他人打开网址即可看到这次结果；回放不会再次调用模型。</small></span><button onClick={()=>setTab("history")}>查看历史实验 →</button></div>}
        <div className="stats"><article><label>用户宇宙</label><strong>{data.users.length}</strong><em>本轮抽样 {config.population} 人</em></article><article><label>Profile Ready</label><strong>{ready}</strong><em>{pct(ready/data.users.length)} 画像达标</em></article><article><label>关系机会</label><strong>{data.matches.length}</strong><em>{gated} 通过 Social Gate</em></article><article><label>双向确认率</label><strong>{pct(mutual)}</strong><em>{groups} 个四人群</em></article></div>
        <div className="overview-grid"><article className="card universe"><CardHead title="用户宇宙" sub="人设、Agent 与当前状态" action="查看全部 →" onClick={()=>setTab("users")}/><div className="orbital">{data.users.slice(0,60).map((x,i)=><button aria-label={x.name} title={`${x.name} · ${x.status}`} onClick={()=>{setSelectedUser(x.id-1);setTab("users")}} key={x.id} className={x.status} style={{"--i":i,"--total":Math.min(60,data.users.length)} as React.CSSProperties}/>) }<div className="core">{active}<small>活跃中</small></div></div><div className="legend"><span><i className="blue"/>聊天中 {data.users.filter(x=>x.status==="chatting").length}</span><span><i className="cream"/>可匹配 {data.users.filter(x=>x.status==="ready").length}</span><span><i className="dim"/>休眠 {data.users.filter(x=>x.status==="resting").length}</span></div></article>
        <article className="card pipeline"><CardHead title="本轮漏斗" sub={`${config.seed} · 已完成`} badge="REPLAYABLE"/>{[["Profile Ready",ready,ready/data.users.length],["Intent Gate",gated,gated/Math.max(1,ready)],["机会生成",data.matches.length,data.matches.length/Math.max(1,gated)],["双边通过",groups,groups/Math.max(1,data.matches.length)],["四人群",groups,1]].map(([n,v,p])=><div className="funnel" key={n as string}><span>{n}</span><div><i style={{width:pct(p as number)}}/></div><strong>{v}</strong></div>)}<div className="gate-note"><b>ⓘ</b><span>Unknown ≠ Yes<br/><small>Intent 不充能，只决定是否进入社交引擎。</small></span></div></article></div>
        <div className="lower-grid"><article className="card compact"><CardHead title="正在发生" sub="最新系统事件" action="全部日志 →" onClick={()=>setTab("logs")}/><EventList events={data.events.slice(0,5)}/></article><article className="card compact"><CardHead title="最近的关系机会" sub="Sequential reveal · 非 Top-K" action="匹配实验 →" onClick={()=>setTab("matches")}/>{data.matches.slice(0,3).map((m,i)=><button className="match-row" key={m.id} onClick={()=>{setSelectedMatch(i);setTab("matches")}}><Avatars a={m.a.name} b={m.b.name}/><span><b>{m.a.name} × {m.b.name}</b><small>{m.relation} · {m.resonance}</small></span><strong>{Math.round(m.score*100)}</strong></button>)}</article></div>
      </>}
      {tab==="history"&&<article className="card history">
        <div className="history-head"><div><h2>共享实验历史</h2><p>点击实验会在本页展开完整对话、画像与匹配；查看回放不消耗 API。</p></div><button className="ghost" onClick={()=>refreshSharedRuns(false)} disabled={historyLoading}>{historyLoading?"刷新中…":"↻ 刷新"}</button></div>
        {runError&&<div className="run-error">{runError}</div>}
        <div className="history-grid">{sharedRuns.length?sharedRuns.map(run=>{const status=savedRunStatus(run),meta=savedRunConfig(run);return <button key={run.id} className={`history-card ${activeRunId===run.id?"selected":""}`} onClick={()=>loadSharedRun(run.id,false)} disabled={historyLoading}><span className={`run-state ${status}`}>{status==="completed"?"已完成":status==="partial"?"部分完成":status==="running"?"运行中":status==="aborted"?"已停止":"运行失败"}</span><time>{new Date(run.createdAt).toLocaleString("zh-CN",{hour12:false})}</time><h3>{meta.experimentName||`实验 ${run.id.slice(-6)}`}</h3><p>{run.population} 人 · {run.rounds} 轮 = {run.rounds*2}–{run.rounds*6} 个气泡/Variant · {run.configJson.includes('"abEnabled":true')?"A/B":"单版本"}</p><div><span>Input <b>{(run.inputTokens||0).toLocaleString()}</b></span><span>Output <b>{(run.outputTokens||0).toLocaleString()}</b></span><span>Cost <b>${((run.estimatedCostMicros||0)/1e6).toFixed(4)}</b></span></div><em>{activeRunId===run.id?"详情已展开":"打开完整详情 →"}</em></button>}):<div className="empty-history"><b>还没有共享实验</b><span>完成第一轮真实实验后，它会自动出现在这里。</span></div>}</div>
        {activeSharedRun&&sharedView&&<section className="history-detail"><div className="history-detail-head"><div><label>EXPERIMENT DETAIL</label><h2>{activeRunMeta.experimentName||experimentName}</h2><p>{new Date(activeSharedRun.createdAt).toLocaleString("zh-CN",{hour12:false})} · {activeSharedRun.population} 人 · {activeSharedRun.rounds} 轮</p></div><div><button onClick={()=>setTab("users")}>用户画像视图</button><button onClick={()=>setTab("matches")}>匹配视图</button></div></div><div className="detail-stats"><span>实际参与者<b>{data.users.length}</b></span><span>已生成画像<b>{data.users.filter(x=>x.memories.length>0).length}</b></span><span>完整消息<b>{data.users.reduce((n,x)=>n+x.transcript.length,0)}</b></span><span>匹配机会<b>{data.matches.length}</b></span></div><div className="history-users"><h3>逐用户完整对话</h3>{data.users.map(persona=><details key={persona.id}><summary><Avatar name={persona.name}/><span><b>{persona.name}</b><small>{persona.city} · {persona.job}</small></span><em>{persona.transcript.length} 条消息</em><strong>{persona.memories.length?"画像已生成":persona.transcript.length?"对话未完成":"未开始"}</strong></summary><div className="history-user-body"><p>{persona.summary}</p>{persona.transcript.length?<div className="transcript">{persona.transcript.map((message,index)=><p className={message.from} key={index}><b>{message.from==="agent"?"小精灵":"用户"}</b>{message.text}</p>)}</div>:<div className="no-transcript">本次实验没有生成该用户的消息。</div>}</div></details>)}</div><div className="history-matches"><h3>匹配结果</h3>{data.matches.length?data.matches.map(item=><div key={item.id}><Avatars a={item.a.name} b={item.b.name}/><span><b>{item.a.name} × {item.b.name}</b><small>{item.relation} · {item.resonance||"关系研究已保存"}</small></span><strong>{Math.round(item.score*100)}</strong></div>):<p>这次实验尚未生成匹配结果。</p>}</div></section>}
        {activeSharedRun&&sharedView&&<HistoryFailures failures={runFailures} users={data.users} legacyPartial={activeSharedRun.status==="partial"}/>}
        <div className="history-note"><b>隐私边界</b><span>实验对话、画像、失败原因和匹配结果会共享；DeepSeek Key 永远不会写入实验记录。</span></div>
      </article>}
      {tab==="run"&&<div className="run-layout"><article className="card controls"><CardHead title="实验设置" sub={`从 ${universeSize} 人宇宙中抽样`} badge="LLM"/><div className={`provider ${hasProvider?"connected":"missing"}`}><i/><span><b>{hasProvider?`${PROVIDER_OPTIONS.find(x=>x.id===provider)?.label||provider} 已连接${sessionKey?" · 当前会话 Key":" · 环境 Secret"}`:"需要配置当前 Provider 的 API Key"}</b><small>{sessionKey?"刷新页面即清除，不写数据库或日志":providerReady?"服务端默认密钥已就绪":"可在下方输入临时 Key"}</small></span><button className="key-toggle" onClick={()=>setShowKey(!showKey)}>{showKey?"收起":hasProvider?"更换":"配置"}</button></div>{showKey&&<div className="session-key"><label>当前 Provider 临时 Key<input type="password" autoComplete="off" placeholder="API Key · 仅保存在页面内存" value={sessionKey} onChange={e=>setSessionKey(e.target.value)}/></label><div><button onClick={()=>setSessionKey("")}>清除临时 Key</button><small>临时 Key 优先于站点环境变量</small></div></div>}<label className="field"><span>实验名称 <small>会显示在共享历史中</small></span><input maxLength={48} value={experimentName} onChange={e=>setExperimentName(e.target.value)} placeholder="例如：Prompt v2 · 20人首测"/></label><label className="field"><span>实验种子 <small>相同人设可复现</small></span><input value={config.seed} onChange={e=>update("seed",e.target.value)}/></label><Slider label="本轮抽样人数" value={config.population} min={1} max={universeSize} step={1} suffix={` / ${universeSize} 人`} onChange={v=>update("population",v)}/><Slider label="每人对话轮次（每轮=用户+Agent）" value={config.rounds} min={2} max={20} step={1} suffix={` 轮 · 通常 ${config.rounds*2} 个气泡/Variant`} onChange={v=>update("rounds",v)}/><Slider label="建群后用户对话轮次" value={config.groupRounds} min={1} max={10} step={1} suffix={` 轮 · 每轮两位用户各发一次`} onChange={v=>update("groupRounds",v)}/><Slider label="并发数" value={concurrency} min={1} max={6} step={1} suffix="" onChange={setConcurrency}/><Slider label="Readiness 阈值" value={config.threshold} min={.3} max={.9} step={.01} format={pct} onChange={v=>update("threshold",v)}/><div className="ab-switch"><span><b>匹配后自动双向确认并建群</b><small>用于验证完整匹配链路；关闭后只生成关系机会</small></span><button className={config.autoGroup?"on":""} onClick={()=>update("autoGroup",!config.autoGroup)}><i/></button></div><div className="ab-switch"><span><b>A/B 对照实验</b><small>相同人设分别运行两个版本</small></span><button className={abEnabled?"on":""} onClick={()=>setAbEnabled(!abEnabled)}><i/></button></div><Variant label="A" model={modelA} prompt={promptA} setModel={setModelA} setPrompt={setPromptA}/>{abEnabled&&<Variant label="B" model={modelB} prompt={promptB} setModel={setModelB} setPrompt={setPromptB}/>}<div className="mode"><span>运行方式</span><button className={runMode==="real"?"selected":""} onClick={()=>setRunMode("real")}>真实 LLM</button><button className={runMode==="synthetic"?"selected":""} onClick={()=>setRunMode("synthetic")}>零成本 dry run</button></div>{runError&&<div className="run-error">{runError}</div>}<button className="start" disabled={running||(runMode==="real"&&!hasProvider)} onClick={runMode==="real"?realRun:dryRun}>{running?`运行中 · ${progress}%`:`▶ 运行 ${config.population} 人${abEnabled&&runMode==="real"?" × A/B":""}`}</button>{running&&<button className="stop" onClick={()=>abortRef.current?.abort()}>停止实验</button>}<p className="budget-note">100 人建议并发 2–3；请保持页面打开。中断会话也会基于现有消息生成阶段性画像；达到阈值的用户会进入候选配对。</p></article>
      <div className="run-main"><article className="card simulation"><CardHead title="运行现场" sub={running?phase:activeRunId?`Run ${activeRunId.slice(-8)} 已完成`:"等待开始真实实验"} badge={running?"LIVE":activeRunId?"SAVED":"READY"}/><div className="progress"><i style={{width:`${progress}%`}}/></div><div className="usage-strip"><div><span>API CALLS</span><b>{realUsage.calls}</b></div><div><span>INPUT</span><b>{realUsage.input.toLocaleString()}</b></div><div><span>OUTPUT</span><b>{realUsage.output.toLocaleString()}</b></div><div><span>EST. COST</span><b>${(realUsage.micros/1e6).toFixed(4)}</b></div></div><div className="flow">{[["01","Conversation","双 Agent 多轮对话"],["02","Memory","Session compaction"],["03","Profile","证据化字段"],["04","Social Gate","Intent × Energy"],["05","Matching","双边关系研究"],["06","Outcome","Day 0/3/7/30"]].map((x,i)=><div className={progress>i*18?"passed":""} key={x[0]}><small>{x[0]}</small><b>{x[1]}</b><span>{x[2]}</span></div>)}</div><div className="live-console"><div className="console-top"><span>LIVE DEEPSEEK TRACE</span><small>{running?phase:realEvents.length?`${realEvents.length} events`:"等待运行"}</small></div><EventList events={(realEvents.length?realEvents:data.events).slice(0,9)}/></div></article><article className="card compact assumptions"><CardHead title="实验解释" sub="避免同模型自嗨与错误归因"/><ul><li><b>Persona</b><span>命理只作冷启动先验；真实问题可自由发散</span></li><li><b>A/B</b><span>相同人设分别跑 A/B，减少样本差异</span></li><li><b>Memory</b><span>每项保留 evidence、confidence、recency、permission</span></li><li><b>Cost</b><span>按每次 API 返回的 usage 实时累计</span></li><li><b>Outcome</b><span>Day 0/3/7/30 持久化回流，而非只看点击</span></li></ul></article></div></div>}
      {tab==="users"&&<div className="split"><article className="card table-card"><div className="universe-controls"><span><b>用户宇宙</b><small>池子大小不会触发模型调用</small></span><input aria-label="用户宇宙规模" type="range" min="20" max="300" step="20" value={universeSize} onChange={e=>resizeUniverse(Number(e.target.value))}/><strong>{universeSize} 人</strong><em>本轮选择 {config.population} 人</em></div><div className="table-tools"><div className="search">⌕ <input placeholder="搜索名字、城市、职业、话题…" value={query} onChange={e=>setQuery(e.target.value)}/></div><span>{filteredUsers.length} / {data.users.length}</span></div><div className="person-table"><div className="tr head"><span>用户 / Agent</span><span>Social Intent</span><span>Readiness</span><span>Energy</span><span>状态</span></div>{filteredUsers.map(x=><button className={`tr ${u?.id===x.id?'selected':''}`} key={x.id} onClick={()=>setSelectedUser(x.id-1)}><span><Avatar name={x.name}/><b>{x.name}<small>{x.city} · {x.job}</small></b></span><span><i className={`intent ${x.intent}`}/>{intentLabel[x.intent]}</span><span><Meter value={x.readiness}/><small>{pct(x.readiness)}</small></span><span>{x.energy.toFixed(1)} / {config.energyMax}</span><span className={`status ${x.status}`}>{x.status==="ready"?"可匹配":x.status==="chatting"?"聊天中":"休眠"}</span></button>)}</div></article>{u&&<article className="card inspector"><div className="identity"><Avatar name={u.name} large/><div><h2>{u.name}</h2><p>{u.age} 岁 · {u.city} · {u.job}</p></div><span className={`status ${u.status}`}>{u.status==="ready"?"可匹配":u.status==="chatting"?"聊天中":"休眠"}</span></div><div className="inspect-metrics"><div><span>Readiness</span><b>{pct(u.readiness)}</b><Meter value={u.readiness}/></div><div><span>Energy</span><b>{u.energy.toFixed(1)}</b><Meter value={u.energy/config.energyMax}/></div><div><span>Evidence</span><b>{pct(u.confidence)}</b><Meter value={u.confidence}/></div></div><section className="profile-block"><label>AGENT 的当前理解</label><p>{u.summary}</p></section><section className="profile-block"><label>MEMORY / PROFILE</label>{u.memories.map((m,i)=><div className="memory" key={m}><i>{i===0?"◷":i===1?"◇":"⌁"}</i><span>{m}<small>{i===0?"Active State · 7 天过期":i===1?"Stable Profile · confidence 0.82":"Social Preference · user confirmed"}</small></span></div>)}</section><section className="profile-block"><label>最近一次对话 · {u.scenario}</label><div className="transcript">{u.transcript.map((m,i)=><p className={m.from} key={i}><b>{m.from==="agent"?"苔苔":"用户"}</b>{m.text}</p>)}</div></section><div className="permission"><span>Discoverable</span><b>{u.discoverable?"ON":"OFF"}</b><small>完整 Profile 不进入发现层</small></div></article>}</div>}
      {tab==="matches"&&<div className="match-layout">
        <article className="card match-list"><CardHead title="关系机会池" sub={`${data.matches.length} 个 latent opportunities`} badge="NO INBOX"/>{data.matches.map((m,i)=><button key={m.id} className={selectedMatch===i?"selected":""} onClick={()=>setSelectedMatch(i)}><Avatars a={m.a.name} b={m.b.name}/><span><b>{m.a.name} × {m.b.name}</b><small>{m.relation} · {m.a.city===m.b.city?"同城":"跨城"}</small></span><em className={m.status}>{m.status==="group"?"已建群":m.status==="one-sided"?"单边确认":"研究中"}</em><strong>{Math.round(m.score*100)}</strong></button>)}</article>
        {match&&<article className="card match-detail">
          <div className="match-hero"><Avatars a={match.a.name} b={match.b.name} large/><p className="eyebrow">SOCIAL OPPORTUNITY #{String(match.id).padStart(3,"0")}</p><h2>{match.a.name} <i>×</i> {match.b.name}</h2><p>{match.relation} relationship hypothesis</p><div className="score-ring" style={{"--score":`${match.score*360}deg`} as React.CSSProperties}><b>{Math.round(match.score*100)}</b><small>fit score</small></div></div>
          <div className="research-grid"><section><label>RESONANCE · 共振</label><p>{match.resonance}</p></section><section><label>MUTUAL VALUE · 双边价值</label><p>{match.value}</p></section></div>
          <section className="evidence"><label>可追溯证据</label>{match.evidence.map(x=><span key={x}>✓ {x}</span>)}</section>
          <div className="consent"><div><Avatar name={match.a.name}/><span>{match.a.name}<small>Agent sequential reveal</small></span><b className={match.aYes?"yes":"wait"}>{match.aYes?"✓ 想认识":"等待"}</b></div><div><Avatar name={match.b.name}/><span>{match.b.name}<small>Inbound Judge</small></span><b className={match.bYes?"yes":"wait"}>{match.bYes?"✓ 想认识":"等待"}</b></div></div>
          {match.status==="group"?<div className="group-created"><b>✦ 四人小群已建立</b><span>{match.a.name} · {match.a.name}的守护者 · {match.b.name} · {match.b.name}的守护者</span>{match.groupTranscript.length>0&&<details className="group-chat" open><summary>完整模拟群聊 · {match.groupTranscript.length} 条</summary><div>{match.groupTranscript.map((message,index)=>{const isA=message.speaker.endsWith("_a"),isAgent=message.speaker.startsWith("agent");const name=message.speaker==="user_a"?match.a.name:message.speaker==="user_b"?match.b.name:message.speaker==="agent_a"?`${match.a.name}的守护者`:`${match.b.name}的守护者`;return <p className={`${isA?"side-a":"side-b"} ${isAgent?"group-agent":"group-user"}`} key={index}><b>{name}</b><span>{message.content}</span></p>})}</div></details>}</div>:<div className="waiting">达到 Readiness 阈值后进入候选池；Agent 认为值得认识且双方确认后才建立四人群。</div>}
          {match.remoteId&&<div className="outcome-panel"><label>LONG-TERM OUTCOME 回流</label><div><button onClick={()=>recordOutcome("day0",true)}>Day 0 双向接受</button><button onClick={()=>recordOutcome("day3",true)}>Day 3 有对话</button><button onClick={()=>recordOutcome("day7",true)}>Day 7 仍持续</button><button onClick={()=>recordOutcome("day30",false)}>Day 30 已结束</button></div></div>}
        </article>}
      </div>}
      {tab==="logs"&&<article className="card logs"><div className="log-head"><div><h2>事件日志</h2><p>{data.events.length} events · {config.seed}</p></div><div>{["ALL","MEMORY","PROFILE","INTENT","ENERGY","MATCH","GROUP"].map(f=><button className={logFilter===f?"active":""} onClick={()=>setLogFilter(f)} key={f}>{f}</button>)}</div><button className="ghost" onClick={exportRun}>⇩ 导出 JSON</button></div><div className="log-columns"><span>TIME</span><span>EVENT</span><span>ENTITY</span><span>DETAIL</span></div>{data.events.filter(e=>logFilter==="ALL"||e.type.includes(logFilter)).map(e=><div className="log-row" key={e.id}><time>{e.time}</time><span className={`event-chip ${e.tone}`}>{e.type}</span><b>{e.user}</b><p>{e.message}</p></div>)}</article>}
    </section>
  </main>
}

function CardHead({title,sub,action,onClick,badge}:{title:string;sub:string;action?:string;onClick?:()=>void;badge?:string}){return <div className="card-head"><div><h2>{title}</h2><p>{sub}</p></div>{action&&<button onClick={onClick}>{action}</button>}{badge&&<b className="live">{badge}</b>}</div>}
function Slider({label,value,min,max,step,suffix="",format,onChange}:{label:string;value:number;min:number;max:number;step:number;suffix?:string;format?:(v:number)=>string;onChange:(v:number)=>void}){const sample=label==="本轮抽样人数";return <label className="slider"><span>{label}<b>{format?format(value):value+suffix}</b></span><input type="range" value={value} min={sample?1:min} max={max} step={sample?1:step} onChange={e=>onChange(Number(e.target.value))}/>{label==="Readiness 阈值"&&<small style={{display:"block",color:"#62676b",fontSize:9,lineHeight:1.55,marginTop:7}}>建议 62%：至少 3 个证据维度，并包含社交意图或关系偏好；50%–61% 仅作探索。</small>}</label>}
function HistoryFailures({failures,users,legacyPartial}:{failures:RunFailure[];users:Persona[];legacyPartial:boolean}){if(!failures.length&&!legacyPartial)return null;return <section className="history-users"><h3>失败原因</h3>{failures.length?failures.map((failure,index)=>{const names=failure.personaId==="SYSTEM"?"整个实验":failure.personaId.split(",").map(id=>users.find(x=>String(x.id)===id)?.name||`用户 ${id}`).join(" × ");return <div className="no-transcript" style={{marginBottom:8}} key={failure.id||index}><b>{names} · Variant {failure.variant}</b><span style={{display:"block",color:"#7796ac",fontSize:9,marginTop:4}}>{failure.stage}</span><p style={{marginTop:7}}>{failure.message}</p></div>}):<div className="no-transcript">这次实验发生在失败原因持久化功能上线前；可确认有任务失败，但当时没有保存服务商返回的具体错误。</div>}</section>}
function Meter({value}:{value:number}){return <i className="meter"><i style={{width:pct(value)}}/></i>}
function Avatar({name,large=false}:{name:string;large?:boolean}){const colors=["#6398c6","#9b8064","#8076a6","#709583","#ae7882"];return <i className={`avatar ${large?"large":""}`} style={{background:colors[hash(name)%colors.length]}}>{name.slice(0,1)}</i>}
function Avatars({a,b,large=false}:{a:string;b:string;large?:boolean}){return <span className={`avatars ${large?"large":""}`}><Avatar name={a} large={large}/><Avatar name={b} large={large}/></span>}
function EventList({events}:{events:SimEvent[]}){return <div className="events">{events.map(e=><div key={e.id}><i className={e.tone}/><time>{e.time}</time><span><b>{e.user}</b><small>{e.message}</small></span></div>)}</div>}
function ProviderPicker({provider,onChange}:{provider:ProviderName;onChange:(x:ProviderName)=>void}){return <article className="card" style={{marginBottom:18,padding:16}}><div className="card-head"><div><h2>LLM Provider</h2><p>支持 OpenAI-compatible Chat Completions；API Key 只用于当前 Provider。</p></div><select aria-label="LLM Provider" value={provider} onChange={e=>onChange(e.target.value as ProviderName)} style={{background:"#202024",border:"1px solid #303036",color:"#bbb",borderRadius:9,padding:"8px 10px"}}>{PROVIDER_OPTIONS.map(x=><option value={x.id} key={x.id}>{x.label}</option>)}</select></div></article>}
function Variant({label,model,prompt,setModel,setPrompt}:{label:string;model:string;prompt:string;setModel:(x:string)=>void;setPrompt:(x:string)=>void}){return <div className="variant"><b>VARIANT {label}</b><label>模型<input aria-label={`Variant ${label} 模型`} value={model} onChange={e=>setModel(e.target.value)} style={{background:"#202024",border:"1px solid #303036",color:"#bbb",borderRadius:8,padding:7,fontSize:10,width:"100%"}}/></label><label>Prompt<select value={prompt} onChange={e=>setPrompt(e.target.value)}><option value="guardian-natural-v3">guardian-v3 · 微信长期陪伴</option><option value="guardian-v1">guardian-v1 · 温和承接（旧）</option><option value="guardian-v2">guardian-v2 · 独立朋友（旧）</option></select></label></div>}
